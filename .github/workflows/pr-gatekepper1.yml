name: PR Gatekeeper

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Print target branch
        run: echo "Merging into ${{ github.base_ref }}"

      - name: Print PR number
        run: echo "PR number is ${{ github.event.pull_request.number }}"

      - name: Finish
        run: echo "Gatekeeper checks complete"